Laravel Zero
================

```bash
locale-gen en_CA en_CA.UTF-8 en_US en_US.UTF-8
apt-get update && apt-get upgrade && apt-get dist-upgrade && apt-get autoremove && apt-get autoclean
wget https://github.com/Lykegenes/vps-toolkit/releases/download/v1.0.0-alpha.1/vps-toolkit-v1.0.0-alpha.1.phar
apt-get -y install software-properties-common

add-apt-repository ppa:ondrej/php
apt-get update
apt-get -y install php7.2
```
